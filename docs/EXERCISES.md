# 発展課題集（Laravelらしい書き方・運用を学ぼう）

バグ修正（[ISSUES.md](ISSUES.md)）が終わった人向けの課題です。
ここでは、「動けばOK」ではなく、「Laravelらしい・保守しやすいコード」を書くことを目標としています。
1課題 =1ブランチ = 1PR推奨。

前提：`php artisan migrate --seed` 済み（`users.role` と `member_logs` が入ります）。
ログイン：管理者 `admin@example.com` / スタッフ `staff@example.com`（どちらも `password`）。

---

## A. 認可・バリデーション

### E1. ポリシー（認可）を追加する 🆕scaffold
**現状**: ログインさえすれば **staff でも会員を削除・編集できてしまう**（権限差が効いていない）。
**やること**: LarabelのPolicyを使って、権限を設定しましょう。
**完了条件**: staffでログインすると削除ボタンが出ない/ URLを直接叩いても403になる/adminは今まで通り操作できる。

### E2. バリデーションをFormValidationに切り出す
> Laravel でいう **Form Request Validation**。実体は `FormRequest` クラス（`php artisan make:request`）。
> Controller直書きの検証を、フォーム専用のバリデーションクラスへ移す課題。

**現状**: `store` はバリデーションが Controller直書き（`$request->validate([...])`）、`update` は無し。
**やること**: FormRequestを使って、入力チェックを専用クラスにまとめましょう。
**完了条件**: 登録・編集の両方で入力チェックが効き、Controllerから検証コードが消える。

> 参考: [teacher/refactoring.md](../teacher/refactoring.md) の FormRequest節（研修生は読んじゃダメ⚠️）

### E3. クエリパラメータのバリデーションを設定する
**現状**: 一覧の `keyword` / `status` / `sort` / `order` / `page` をそのままで使っている。そのため、想定外の値が送られると問題が起こる可能性があります。
**やること**: URLパラメータも入力チェックしましょう。
**完了条件**: 変な値をURLに入れても、安全に動作する、情報漏洩にならない。

---

## B. 設計・コード品質

### E4. タイプヒンティングを追加する
**現状**: メソッドの引数・戻り値に型がほぼ無い。
**やること**: メソッドの引数や戻り値に型を書きましょう。
**完了条件**: 主要メソッドに型が付き、IDE補完・静的解析が効くので、使いやすくなります。

### E5. 列挙型（enum）で状態を表す 🆕(PHP8.1)
**現状**: `status(0/1/2)` `rank(1/2/3)` `gender(1/2/3)` がマジックナンバー。
**やること**: PHP 8.1 の `enum`（または定数クラス）に集約し、`MemberStatus::Active`のようにかけるようにしましょう。
**完了条件**: マジックナンバーが消えていること。
> ※ enumを使うため Dockerを PHP 8.1に上げる（[teacher/exercises-answers.md](../teacher/exercises-answers.md) 参照）。

### E6. リレーションを使ってデータを取得する 🆕scaffold
**現状**: 会員の操作履歴テーブルがありますが、リレーションを活用していません。
**やること**: MemberとMemberLogを関連付けましょう。
**完了条件**: リレーションを使った取得ができていて、履歴のある会員だけに絞り込める／件数が N+1 なしで表示できる。

### E7. Controllerが持っている便利な機能を使いこなす
**現状**: Laravelには最初から便利な機能がありますが、あまり使っていません。
**やること**: コントローラ由来の `authorize()`・`middleware()`・`validate()` などを使ってみましょう。
**完了条件**: Laravelの便利に気づく。

### E8. 共通機能を切り出す
**現状**: 同じような処理が複数箇所にある。
**やること**: Serviceなどを使って、共通の処理を1か所にまとめましょう。
**完了条件**: 同じコードを書かなくてよくなる。
> 参考: [teacher/refactoring.md](../teacher/refactoring.md) の Service節

---

## C. インフラ設定

### E9. ログインの回数制限を設定しよう
**現状**: ログインを何回でも試すことができます。
**やること**: Laravelのレート制限を設定しましょう。（例：1分間に５回まで）
**完了条件**: 短時間に大量リクエストすると429エラー。

### E10. PHP の容量設定を変更する
**現状**: 大きめの画像（例: 5MB超）をアップロードすると失敗する。
**やること**: `upload_max_filesize` / `post_max_size` / `memory_limit` を調整する（この教材では `docker/php/` の ini）。
**完了条件**: 想定サイズの画像がアップロードできる。設定値の意味を説明できる。

---

## D. 調査（デバッグ練習）

### E11. dd()を使って原因を調べる
**現状**: ランクで「ゴールド」を選んでも、必ず「通常」で登録されます。
**やること**: `dd()` / `dump()` を使って原因を特定する。
1. フォームからどんなデータが送られているか
2. Controllerでは何を受け取っているか
を確認して原因を見つけましょう。
**完了条件**: 原因を特定できる/ 正しく修正できる /コミット前にはdd()を必ず削除する

---

## 取り組み順の目安
E1 権限
E2 入力チェック
E3 URLパラメータ
E7 Laravelの便利機能
E4 型
E5 enum
E8 共通化
E6 リレーション
E9 レート制限
E10 PHP設定

E11（dd）はいつ挑戦してもOKです。デバッグの筋トレとして取り組んでみましょう💪